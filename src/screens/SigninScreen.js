import React from "react";
import { StyleSheet, View, Text } from "react-native";

const SigninScreen = () => {
  return <Text style={{ fontSize: 50 }}>SigninScreen</Text>;
};

const styles = StyleSheet.create({});

export default SigninScreen;
